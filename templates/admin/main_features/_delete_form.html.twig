<form method="post" action="{{ path('main_features_delete', {'id': main_feature.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ main_feature.id) }}">
    <button class="btn">Delete</button>
</form>
