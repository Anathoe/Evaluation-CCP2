{% extends 'base.admin.html.twig' %}

{% block title %}MainFeatures{% endblock %}

{% block body %}
    <h1>MainFeatures</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ main_feature.id }}</td>
                <td>{{ main_feature.image }}</td>
            </tr>
            <tr>
                <th>TitleFeature</th>
                <td>{{ main_feature.TitleFeature }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('main_features_index') }}">back to list</a>

    <a href="{{ path('main_features_edit', {'id': main_feature.id}) }}">edit</a>

    {{ include('admin/main_features/_delete_form.html.twig') }}
{% endblock %}
