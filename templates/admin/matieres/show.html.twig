{% extends 'base.admin.html.twig' %}

{% block title %}Matieres{% endblock %}

{% block body %}
    <h1>Matieres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matiere.id }}</td>
            </tr>
            <tr>
                <th>Matiere</th>
                <td>{{ matiere.Matiere }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('matieres_index') }}">back to list</a>

    <a href="{{ path('matieres_edit', {'id': matiere.id}) }}">edit</a>

    {{ include('admin/matieres/_delete_form.html.twig') }}
{% endblock %}
