{% extends 'base.html.twig' %}

{% block title %}Men{% endblock %}

{% block body %}
    <h1>Men</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ man.id }}</td>
            </tr>
            <tr>
                <th>TitleShop</th>
                <td>{{ man.TitleShop }}</td>
            </tr>
            <tr>
                <th>SubtitleShop</th>
                <td>{{ man.SubtitleShop }}</td>
            </tr>
            <tr>
                <th>TitleEx</th>
                <td>{{ man.titleEx }}</td>
            </tr>
            <tr>
                <th>TextEx</th>
                <td>{{ man.TextEx }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('men_index') }}">back to list</a>

    <a href="{{ path('men_edit', {'id': man.id}) }}">edit</a>

    {{ include('admin/men/_delete_form.html.twig') }}
{% endblock %}
